return {
    settings = {
        bashIde = {
            -- Narrow the startup workspace scan; the default globs the whole tree.
            globPattern = "*@(.sh|.inc|.bash|.command)",
        },
    },
}
