-- Copy this file to `langs_local.lua` (gitignored) and uncomment lines for
-- languages you do not want loaded. Anything omitted falls through to the
-- defaults in `langs.lua` (all enabled).
return {
    -- asm         = false,
    -- bash        = false,
    -- ["c-cpp"]   = false,
    -- cmake       = false,
    -- dockerfile  = false,
    -- elixir      = false,
    -- go          = false,
    -- graphql     = false,
    -- helm        = false,
    -- json        = false,
    -- just        = false,
    -- lua         = false,
    -- markdown    = false,
    -- nix         = false,
    -- ocaml       = false,
    -- protobuf    = false,
    -- python      = false,
    -- ron         = false,
    -- rust        = false,
    -- shader      = false,
    -- sql         = false,
    -- terraform   = false,
    -- toml        = false,
    -- typst       = false,
    -- web         = false,
    -- yaml        = false,
    -- zig         = false,
}
